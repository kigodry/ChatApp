<Window x:Class="ChatHello.ChatLogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatHello"
        mc:Ignorable="d"
        Title="ChatLogsWindow" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Отправитель" Binding="{Binding Sender}" Width="*" />
                <DataGridTextColumn Header="Текст" Binding="{Binding Text}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
